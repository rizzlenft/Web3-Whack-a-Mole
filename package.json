{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "pnpm --filter @workspace/whack-a-mole dev",
    "dev:game": "pnpm --filter @workspace/whack-a-mole dev",
    "build:game": "pnpm --filter @workspace/whack-a-mole build",
    "build:game:rizzle": "BASE_PATH=/games/whack-a-mole/ pnpm --filter @workspace/whack-a-mole build",
    "typecheck:game": "pnpm --filter @workspace/whack-a-mole typecheck",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "volta": {
    "node": "22.22.3"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
